# Backend (Express server) URL, no trailing slash.
# Leave empty in local dev — Vite proxies /api and /uploads to localhost:5001.
# On Render, set this to the backend service URL, e.g. https://inkbook-server.onrender.com
VITE_API_URL=
